NOTES:
  - .env files:
      - may exist in the following
        - /root
        - /root/app/<project>
  - /apps/website:
      - .env files here may have redundant environment variables for intellisense
