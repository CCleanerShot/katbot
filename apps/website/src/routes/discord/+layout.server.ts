import type { LayoutServerLoad } from "./$types";

export const load: LayoutServerLoad = () => {
    return {
        title: "Discord",
        description: "Related to the discord bot, 'KatBot'."
    }
}